// Export ComponentExample as the default export
export { default } from './ComponentExample';

// Also export the hook for direct access if needed
export { useComponentExample } from './hooks/useComponentExample';