import React from 'react';
import { AvatarProps } from '../../types/display';

/**
 * AvatarAdapted component
 * 
 * An enhanced avatar component for user profile images and placeholders
 * with various shapes, sizes, and fallback options. Automatically generates
 * initials from alt text when no image or children are provided.
 */
declare const AvatarAdapted: React.FC<AvatarProps>;

export default AvatarAdapted;