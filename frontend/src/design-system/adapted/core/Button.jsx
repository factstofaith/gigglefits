import React from 'react';

const Button = (props) => {
  return (
    <div className="button-component">
      Button Component
    </div>
  );
};

Button.displayName = 'Button';

export default Button;
