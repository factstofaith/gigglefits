import React from 'react';
import { Tab as MuiTab } from '@mui/material';

/**
 * Tab component for navigation
 */
const Tab = (props) => {
  // Added display name
  Tab.displayName = 'Tab';

  // Added display name
  Tab.displayName = 'Tab';

  return <MuiTab {...props} />;
};

export default Tab;
